# CMakeLists.txt for the TimeNavigator plugin
SET(TIMENAVIGATOR_VERSION "${VERSION}")

ADD_DEFINITIONS(-DTIMENAVIGATOR_PLUGIN_VERSION="${TIMENAVIGATOR_VERSION}")
ADD_DEFINITIONS(-DTIMENAVIGATOR_PLUGIN_LICENSE="GNU GPLv2 or later")

ADD_SUBDIRECTORY(src)
