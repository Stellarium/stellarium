
SET(TIME_ZONE_CONFIGURATION_MAJOR "0")
SET(TIME_ZONE_CONFIGURATION_MINOR "0")
SET(TIME_ZONE_CONFIGURATION_PATCH "4")
SET(TIME_ZONE_CONFIGURATION_VERSION "${TIME_ZONE_CONFIGURATION_MAJOR}.${TIME_ZONE_CONFIGURATION_MINOR}.${TIME_ZONE_CONFIGURATION_PATCH}")

IF(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)

ADD_DEFINITIONS(-DTIME_ZONE_CONFIGURATION_VERSION="${TIME_ZONE_CONFIGURATION_VERSION}")

ADD_SUBDIRECTORY( src )

INSTALL(FILES DESTINATION "modules/TimeZoneConfiguration")


