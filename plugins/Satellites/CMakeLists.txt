SET(SATELLITES_VERSION "0.7.1")

SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)

ADD_DEFINITIONS(-DSATELLITES_PLUGIN_VERSION="${SATELLITES_VERSION}")

ADD_SUBDIRECTORY( src )
INSTALL(FILES DESTINATION "modules/Satellites")
