#
# Nomenclature for Solar system bodies
#
# Nomenclature data was obtained from the web page https://planetarynames.wr.usgs.gov
#
# One rule per line. Each rule contains six elements with white space (or "tab char") as delimiter.
# Format:
#	planet name				: string
#	ID of surface feature			: unique integer (equals to Feature ID obtained from https://planetarynames.wr.usgs.gov)
#	translatable name of surface feature	: string
#	type of surface feature			: string (2-char code; details: https://planetarynames.wr.usgs.gov/DescriptorTerms)
#	latitude of surface feature		: float (decimal degrees)
#	longitude of surface feature		: float (decimal degrees)
#	size of surface feature			: float (kilometers)
#