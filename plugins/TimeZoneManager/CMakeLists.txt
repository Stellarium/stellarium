
SET(TIMEZONEMANAGER_MAJOR "0")
SET(TIMEZONEMANAGER_MINOR "0")
SET(TIMEZONEMANAGER_PATCH "1")
SET(TIMEZONEMANAGER_VERSION "${TIMEZONEMANAGER_MAJOR}.${TIMEZONEMANAGER_MINOR}.${TIMEZONEMANAGER_PATCH}")

IF(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)

ADD_DEFINITIONS(-DPLUGIN_VERSION="${TIMEZONEMANAGER_VERSION}")

ADD_SUBDIRECTORY( src )

INSTALL(FILES DESTINATION "modules/TimeZoneManager")


