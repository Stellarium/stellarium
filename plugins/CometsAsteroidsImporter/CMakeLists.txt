
SET(CA_IMPORTER_MAJOR "0")
SET(CA_IMPORTER_MINOR "2")
SET(CA_IMPORTER_PATCH "1")
SET(CA_IMPORTER_VERSION "${CA_IMPORTER_MAJOR}.${CA_IMPORTER_MINOR}.${CA_IMPORTER_PATCH}")

IF(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
	SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)

ADD_DEFINITIONS(-DPLUGIN_VERSION="${CA_IMPORTER_VERSION}")

ADD_SUBDIRECTORY( src )

INSTALL(FILES DESTINATION "modules/CAImporter")


