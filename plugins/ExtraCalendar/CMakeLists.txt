# This is the cmake config file for the ExtraCalendar
SET(EXTRACALENDAR_VERSION "0.1.0")
ADD_DEFINITIONS(-DEXTRACALENDAR_PLUGIN_VERSION="${EXTRACALENDAR_VERSION}")
ADD_DEFINITIONS(-DEXTRACALENDAR_PLUGIN_LICENSE="GNU GPLv2 or later")

ADD_SUBDIRECTORY( src )

IF(APPLE)
     SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
     SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)
INSTALL(FILES DESTINATION "modules/ExtraCalendar")
