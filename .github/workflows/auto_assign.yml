#
# See https://github.com/marketplace/actions/auto-assign-action
#
name: 'Auto Assign'
on:
  pull_request_target:
    types: [opened, reopened, ready_for_review]
    paths-ignore:
      - 'skycultures/**'

permissions:
  contents: read

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # the author is set as assignee through the issues API
      issues: write
      pull-requests: write
    if: "!contains(github.actor, 'transifex')"
    name: Add reviews
    steps:
      - uses: kentaro-m/auto-assign-action@0a2c53d3721e4c5cfcce6afd4014aecc337979f6 # v2.0.2
