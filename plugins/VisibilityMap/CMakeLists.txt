# CMakeLists.txt for the VisibilityMap plugin
SET(VISIBILITYMAP_VERSION "${VERSION}")

ADD_DEFINITIONS(-DVISIBILITYMAP_PLUGIN_VERSION="${VISIBILITYMAP_VERSION}")
ADD_DEFINITIONS(-DVISIBILITYMAP_PLUGIN_LICENSE="GNU GPLv2 or later")

ADD_SUBDIRECTORY(src)
