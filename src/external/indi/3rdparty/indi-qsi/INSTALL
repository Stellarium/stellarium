QSI INSTALL
============

You must have CMake >= 2.4.7 in order to build this package.

1) $ tar -xzf indi-qsi.tar.gz
2) $ mkdir indi_qsi_build
3) $ cd indi_qsi_build
4) $ cmake -DCMAKE_INSTALL_PREFIX=/usr . ../indi-qsi
5) $ make
6) $ su -c 'make install' or sudo make install

