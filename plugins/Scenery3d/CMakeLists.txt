SET(SCENERY3D_VERSION "0.14.1")

ADD_DEFINITIONS(-DSCENERY3D_PLUGIN_VERSION="${SCENERY3D_VERSION}")
ADD_DEFINITIONS(-DSCENERY3D_PLUGIN_LICENSE="GNU GPLv2 or later")

ADD_SUBDIRECTORY( src )

IF(APPLE)
     SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
     SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)

INSTALL(FILES DESTINATION "modules/Scenery3d")
