SET(OCULUS_STELMODULE_MAJOR "1")
SET(OCULUS_STELMODULE_MINOR "1")
SET(OCULUS_STELMODULE_PATCH "2")
SET(OCULUS_VERSION "${OCULUS_STELMODULE_MAJOR}.${OCULUS_STELMODULE_MINOR}.${OCULUS_STELMODULE_PATCH}")

IF(APPLE)
    SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/Library/Application\ Support/Stellarium)
ElSE(APPLE)
    SET(CMAKE_INSTALL_PREFIX $ENV{HOME}/.stellarium)
ENDIF(APPLE)

ADD_DEFINITIONS(-DOCULUS_PLUGIN_VERSION="${OCULUS_VERSION}")

ADD_SUBDIRECTORY( src )

INSTALL(FILES DESTINATION "modules/Oculus")
